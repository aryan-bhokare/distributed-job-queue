# Distributed Job Queue — project context

The workspace operating manual (`../../CLAUDE.md`) and global rules auto-load on top of this
file — do not repeat them here. This file holds only what is specific to Distributed Job Queue.

## Session start
Read `PROGRESS.md` and `docs/design.md` first to rehydrate — that's the fast, cheap way to
resume without reloading a long history.

## Project-specific conventions
- (language, module layout, run commands — fill in once design is set)

## Current focus
See `PROGRESS.md`.
